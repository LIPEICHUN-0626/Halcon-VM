# Git 批次记录

更新日期：2026-07-19

## 规则

- 一个“计次”必须是已经验证、可独立说明用户价值的小交付；纯格式、失败尝试和未验证中间状态不计。
- 工作区文件即时保存，累计 10 次后统一执行差异、敏感信息、构建产物和验证检查，再 commit/push 到 `origin/main`。
- 用户明确要求提前上传，或高风险操作需要安全回退点时，可提前提交；必须记录原因并重新确定计数锚点。

## 当前批次

- 上一批锚点：`00af1ad`（S05 统一模块配置和实例参数）
- 已完成批次：S06-S15 共 `10 / 10` 次，2026-07-19 统一完成差异、敏感信息、构建产物、回归和物理 UI 检查，并批量提交推送 `origin/main`。
- 当前新批次计数：`0 / 10`
- 下一候选计次：S16 “真实连接画布首版 + HALCON 图像增强”完成并验证后记为新批次第 1 次；不足 10 次不提交/推送。

| 次数 | 小交付 | 状态 | 验证摘要 |
| --- | --- | --- | --- |
| 1 | S06 ROI 配置工作台 + 逐 ROI 工业结果 | 已验证实现 | 统一配置可绑定/绘制 ROI 并选择合并/逐区；Blob/灰度/边缘两区 HALCON 结果、集合输出、旧配方 Union 默认、空绑定/非法模式、草稿隔离、Debug/Release、启动和 UI 截图通过；按 10 次规则未提交/推送 |
| 2 | S07 输入订阅工作台 + 真实 Region 数据链 | 已验证实现 | 类型化输入卡片、草稿应用/取消、I/O 跳转和默认恢复可见；Blob `SelectedRegion` 真实驱动 Gray/Edge，顺序/类型/上游 NG/生命周期/删除/旧配方/NumericJudge 兼容、Debug/Release、启动和 UI 截图通过；按 10 次规则未提交/推送 |
| 3 | S08 方案/配方工作区 + 自动保存恢复 | 已验证实现 | 顶部方案状态和 `当前/最近/恢复/自检` 四内页可见；v2 JSON、无版本迁移、原子覆盖和单份备份、保存失败正式文件字节不变、含 S07 绑定的恢复、损坏恢复隔离、Debug/Release、启动、HALCON 回归和 UI 截图通过；按 10 次规则未提交/推送 |
| 4 | S09 图像上下文工作台 + 本地图像源/Image 数据链 | 已验证实现 | 全局/模块输入/模块输出、来源、文件/SN、尺寸、像素与叠加状态可见；两图经托管 Image 链得到 Blob `400→800`、Gray `17→34`、Edge `76.109→116.109`，Region 均值 `255`、Number `OK→OK`；顺序/类型/未运行/已释放、重跑失效、生命周期、配方、Debug/Release、启动、94/11 个事件入口和物理 1600×950 UI 通过；按 10 次规则未提交/推送 |
| 5 | S10 停靠式模块配置 + HALCON 图像通道 | 已验证实现 | 首批六类主要工具在右侧常驻配置完成草稿/应用/撤销/试运行，Shape/HDevelop 保留兼容入口；RGB `20/100/200` 抽取、灰度 `87`、非法通道拒绝、下游 Blob `4000`/Gray `92.1`/Edge `136.109`、配方、失效、生命周期、S09 回归、Debug/Release、启动、101/11 个事件入口和物理 1600×950 UI 通过；按 10 次规则未提交/推送 |
| 6 | S11 ROI 精细编辑 + HALCON 图像滤波 | 已验证实现 | 矩形/圆 ROI 控制点、移动/缩放、边界/最小尺寸、复制/绑定、锁定/删除保护和配方回读通过；HALCON 均值冲击像素 `28`、中值 `0`、RGB 3 通道，下游 Blob `1267`/Gray `45.047`/Edge `139.152`，非法模板、失效/释放、S09/S10、旧 v1 配方、Debug/Release、Release 启动、128 个事件入口和物理双截图通过；按 10 次规则未提交/推送 |
| 7 | S12 VM 模块箱效率化 + HALCON 阈值分割 | 已验证实现 | 分类折叠、搜索、收藏、最近、双击追加、拖拽确定性插入和本机状态通过；RGB 两目标阈值 `Area=1792 / RegionCount=2 / rgb1_to_gray`，Gray/Edge Region 订阅、空区域 NG、非法范围、顺序/类型、未运行/释放上游、失效/释放、v2 配方、S09-S11、旧 v1 配方、Debug/Release、Debug 启动和物理 1600×930 UI 通过；按 10 次规则未提交/推送 |
| 8 | S13 图像调试区可视化 + HALCON Region 形态学 | 已验证实现 | 图像工作区放大/原位恢复、ROI/结果独立显隐、真实来源/颜色/对象数/面积图例和空/失效状态通过；阈值面积 `2657`，开/闭/膨胀/腐蚀面积 `2657/2665/3321/1993`，Gray/Edge 订阅、非法半径、顺序/类型、上游 NG/释放、下游失效、v2 配方、S09-S12、旧 v1 配方、Debug/Release、Release 启动、118 个事件入口和物理 1600×930 双截图通过；按 10 次规则未提交/推送 |
| 9 | S14 高级 ROI 编辑 + HALCON Region 特征筛选 | 已验证实现 | 旋转矩形中心/轴端/旋转点、移动/旋转/宽高/边界/复制/锁定/配方与多边形逐顶点/编号/非法几何/锁定/配方通过；HALCON 面积/宽度/高度/圆度筛选分别保留 `396/796/1196/396 px²`，Gray/Edge 订阅、非法范围、顺序/类型、上游未运行/NG/释放、下游失效、v2 配方、S09-S13、旧 v1 配方、Debug/Release、双版本启动、134 个事件入口和物理 1600×930 双截图通过；按 10 次规则本轮不提交/推送 |
| 10 | S15 流程依赖可视化 + HALCON Region 集合运算 | 已验证实现 | 中央卡真实 IN/OUT 端口、来源、值、消费者、直接上下游、双输入汇聚、等待/失效/删除通过；HALCON 并/交/差/对称差面积 `2800/400/1200/2400 px²`，Feature/Gray/Edge、缺一路/同源/类型/顺序/空结果、配方、生命周期、S09-S14、旧配方、Debug/Release、双版本启动和物理 1600×930 UI 通过；达到 10/10，按规则统一提交推送并归零 |
