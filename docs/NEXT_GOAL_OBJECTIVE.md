# 新目标模式 Objective

更新日期：2026-07-16
状态：当前长期 `/goal` 的持续执行目标

## 可直接使用的目标

进入长期产品目标模式，持续把 `D:\codex\codex1` 改造成尽量完整复刻海康机器人 VM / VisionMaster 页面、功能、显示区域和用户习惯的工业视觉开发平台，唯一核心差异是全部视觉算子、图像处理、定位、测量、标定和坐标转换均由 HALCON/HDevEngine 实现。

开始前完整阅读 `AGENTS.md`、`docs/VM_OFFICIAL_REFERENCE_SPEC.md`、`docs/CODEX_MASTER_PROMPT_VM_REPLICA.md`、`docs/PRODUCT_GOAL.md`、`docs/PRODUCT_ROADMAP.md`、`docs/SPRINT_BACKLOG.md`、`docs/PRODUCT_GAP_ANALYSIS.md` 和 `docs/BSB_REQUIREMENTS.md`，并以实际活动代码和可复现验证为准，不得只依赖历史总结。

永久执行规则：

1. VM 始终只指海康机器人 VM / VisionMaster；不做 Cognex、通用 Graph 或只换皮的软件。
2. 以海康官方产品页、VM5.0 发布资料和官方用户手册为产品参照；尽量完整复刻信息架构、主工作区、流程搭建、模块配置、图像/ROI、输入输出订阅、调试运行、资源管理、通信、运行界面、生产运维和工具目录。
3. 不复制海康专有代码、商标和美术资产；使用自制资产实现同类页面与交互。
4. 每轮自动执行 A/B 双线，不再等待逐轮确认：A 线必须有肉眼可见的 VM UI/UX 升级；B 线必须新增可从 UI 配置、运行、保存和验证的真实通用工业功能。
5. 每轮先输出完整计划，然后立即实施；只有恢复 BSB、改变 HALCON 算法含义、触碰相机 SDK、投产协议、旧工程格式或其他高风险边界时才暂停请求授权。
6. BSB 是长期验收场景，但当前按用户指令暂停；收到明确恢复指令前不开发、不验证、不宣称完成。
7. 不得用内部类、空页面、假按钮、Graph 动画、编译通过或模拟数据冒充产品完成。
8. 每轮完成入口、配置、输入、执行、输出/图形、日志/异常、配方保存/回读和测试闭环；执行 Debug/Release x64 编译、启动、HALCON 回归、旧配方兼容、异常路径和高 DPI UI 验证。
9. 每轮按真实代码更新产品目标、路线图、BSB 基线、Sprint、Gap 分析和必要的官方规范，并将已验证更新提交、推送到 GitHub `origin/main`。
10. 长期目标不会因单个 Sprint 完成而结束；只有完整 VM 产品验收矩阵全部满足后才可标记完成。

S04 已完成并验证：活动 WPF 已形成顶部菜单/快捷工具条、左侧模块箱、中央网格顺序流程、右上 HALCON 图像/ROI、右下模块结果/配置和底部状态/历史；运行当前、运行到此、从此运行、单次、连续、暂停/停止、间隔、超时和遇 NG 停止已进入活动执行器与配方。该结果仍只宣称确定性线性执行。

S05 已完成并验证：双击流程节点、流程右键、流程区配置按钮或参数页入口可打开统一模块配置工作台；Blob、灰度、边缘和数值判定具备基本参数、运行参数、输入、ROI/模板、结果显示、帮助以及试运行/应用/确定/取消。Blob/灰度/边缘使用实例参数 Schema，数值判定继续使用实例级订阅/判定 Schema；同类型多实例限制已解除，旧配方顶层视觉参数迁移已验证。Shape/HDevelop 和输入/ROI 深度编辑仍是兼容边界。

下一轮从 S06 开始：

- A 线：把统一配置工作台的 `ROI/模板` 摘要页升级为真实 ROI 图层绑定编辑器，显示 RoiId、名称、形状、显隐、启停和绑定状态；支持草稿绑定/解绑、应用/取消，并能带着当前工具上下文进入右上图像区绘制和管理。右上 ROI 侧栏同步当前工具与逐区状态。
- B 线：为 Blob、灰度和边缘增加 `合并 ROI`（旧配方兼容默认）与 `逐 ROI` 两种实例运行模式；逐 ROI 模式按稳定 RoiId 独立执行现有 HALCON 算法，输出逐区结果码、主数值、消息、错误和耗时集合，并进入结果区、日志和配方。不得改变原合并模式、算法参数含义和坐标约定。
- 验收：至少两个不同区域得到可区分的独立结果；停用/解绑/删除、无绑定、非法模式、应用/取消和旧/新配方回读通过；S01-S05、合并 ROI 与 HALCON 回归不退化；Debug/Release、启动、XAML 入口和 1920×1200 UI 截图通过；Shape/HDevelop 不越过兼容边界；BSB 继续按指令未实施/未验证；完成后提交推送 GitHub 并自动进入下一轮 A/B 双线。
