# 产品差距分析

更新日期：2026-07-13  
目标：海康 VM / VisionMaster 类型工业视觉平台体验 + HALCON 算法内核

## 1. 当前活动架构

- `App.xaml` 启动 `MainWindow.xaml`，因此 WPF 主窗口是唯一活动产品入口。
- `MainWindow` 直接持有图像、ROI、模板、通讯、配方、结果和运行状态，并以事件处理器驱动。
- `MainForm` 和 `TemplateCreateForm` 仍参与编译，但没有活动启动引用；其中相机、Mask、显示框编辑和结果筛选等能力对 WPF 用户不可达。
- 没有统一工具实例模型、参数 Schema、输入输出端口、数据订阅或 BSB 模型。

## 2. 差距矩阵

| 领域 | 当前实际状态 | VM 目标状态 | 差距 | 优先级 |
| --- | --- | --- | --- | --- |
| 主界面 | 顶部按钮、中央图像、右侧页签、底部日志 | 顶栏、工具箱、顺序流程、图像、Inspector、底部信息 | 缺核心工作区结构 | P0 |
| 工具箱 | 无 | 分类、搜索、收藏、最近、双击/拖拽添加 | 完全缺失 | P0 |
| 流程区 | 五个固定复选框，执行顺序硬编码 | 工具实例、排序、状态、耗时、独立/区段运行 | 完全不符合 VM | P0 |
| 参数编辑 | Blob/灰度/边缘永久堆叠；模板弹窗 | 选中工具即显示统一 Inspector | 高度割裂 | P0 |
| 输入输出 | 只有 HDevelop 固定 Image/ROI 和 TCP 结果字符串 | 类型化端口、订阅、值预览、错误定位 | 缺失 | P0 |
| 图像交互 | 缩放、右键平移、适应、矩形/圆/多边形新建 | 原始大小、像素值、控制点编辑、旋转矩形、多图层/多相机 | 部分实现 | P1 |
| 单工具调试 | 模板可单独匹配，其余依赖全流程 | 每个实例可独立运行并显示输入输出 | 部分缺失 | P0 |
| 相机 | 服务层具备 Open/Grab/Continuous；WPF 无入口 | 设备管理、状态、参数、采集和诊断 | 内部结构无用户价值 | P0 |
| 计算工具 | 无 | 数值、逻辑、数组、字符串、脚本 | 缺失 | P1 |
| 坐标工具 | 无 | 标定选择、转换、补偿、图形和异常 | 缺失 | P0 |
| BSB 工具 | 无 | Mark、点集、缓存、坐标、焊点、防呆、PLC、追溯 | 缺失 | P0 |
| TCP | Client/Server、手动收发、自动结果 | 设备、心跳、重连、超时、协议、映射、触发、模拟 | 基础可用、工业闭环缺失 | P1 |
| 配方 | 单 JSON，保存固定字段 | 工程/方案/配方、版本、迁移、自动保存、换型 | 部分实现 | P1 |
| 结果历史 | 内存记录、回看、导出 | 持久历史、SN/载具/批次、图片与坐标追溯 | 生产能力缺失 | P1 |
| 诊断 | HALCON 文件和目录自检 | 相机、PLC、协议、配方、标定、缓存和最近错误 | 很不完整 | P2 |
| 旧工程兼容 | 未发现 `.jw/.handle` 读取代码或样本 | 可打开、迁移、验证并回退 | 无证据，不能宣称完成 | P0 风险 |

## 3. 只有内部结构或不可达的实现

1. `HalconImageService` 的相机采集方法只由不可达 `MainForm` 使用，当前 WPF 用户无法配置和采集。
2. `MainForm` 内较丰富的模板 Mask、显示框控制点、相机和结果过滤没有迁移到活动 WPF。
3. `RoiService` 没有活动引用。
4. `InspectionResultStore.Query` 只在不可达旧窗口中使用；WPF 结果区不能查询筛选。
5. WPF 保存 `LastImageDirectory` 和 `TemplateOptions`，但加载路径没有完整消费这些信息。
6. WPF 保存原图副本，但没有原图/处理中间图/结果图切换入口。

## 4. 已识别的具体风险

- `ApplyRecipe` 在新配方没有有效模板路径时未明确清除上一模板，可能造成配方串用。
- 训练后未另存 `.shm` 的模板没有持久路径，保存配方时无法完整恢复。
- TCP 以每次 `Read` 直接解码消息，没有工业协议分帧；没有连接超时、心跳和自动重连。
- 连续运行基于 UI DispatcherTimer，尚未形成生产线程、取消和节拍治理模型。
- 结果图像快照全部驻留内存，长时间运行存在内存增长风险。
- 当前没有真实 `.jw/.handle`、PLC 地址表、BSB 标定和协议样本，相关兼容性仍缺权威验证材料。

## 5. 当前消差策略

先解决最高用户价值的产品结构缺口：建立 VM 工具箱、顺序流程和 Inspector，同时交付首个可运行的 BSB 坐标工具。随后再建设输入输出订阅、相机设备、ROI 编辑、配方版本、TCP 工业化和 BSB 数据缓存，避免继续堆叠不可见抽象。

每轮以代码入口、用户路径、独立测试、编译/启动和 BSB 场景为完成证据；没有证据的条目保持“未实现”或“部分实现”。

