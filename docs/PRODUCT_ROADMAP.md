# 产品路线图

更新日期：2026-07-19
代码核验基线：S15 已验证源码（2026-07-19 本地验证，S06-S15 批次 10/10 已进入批量交付，新批次 0/10）
官方目标基线：`docs/VM_OFFICIAL_REFERENCE_SPEC.md`

## 状态定义

- **已验证实现**：活动产品入口可用，代码存在且有运行/测试证据。
- **部分实现**：基础能力可用，但缺产品闭环或工业特性。
- **开发中**：已确认并正在本轮实施。
- **提议**：已进入路线图，尚未开始。
- **未实现**：无活动产品能力或只有不可达/内部结构。

## Stage 1-6 代码核验

仓库没有原 Stage 1-6 定义和有效 Git 历史；下表按现存代码能力重建，不冒充原历史标题。

| 能力阶段 | 当前状态 | 代码证据与用户能力 |
| --- | --- | --- |
| 1. 图像加载与显示 | 部分实现 | 活动 WPF 支持图片、目录、拖放、缩放、平移、适应、工作区放大/恢复、空状态和 ROI/结果独立显隐；本地图像源和图像通道可连续输出托管 Image，右上可切换全局/模块输入/模块输出并显示来源、SN、尺寸、像素值及真实结果图例；仍只有单图像视口，相机服务存在但 WPF 无入口 |
| 2. ROI 与模板 | 部分实现 | 中央工具栏支持矩形、圆、旋转矩形、多边形、确认、取消、适应和 1:1；四类 ROI 可直接移动并分别调整边角/半径、旋转与轴长或多边形顶点，支持复制、锁定、边界/非法几何保护、分层、命名、显隐、启停、工具绑定和保存，Shape Model 训练/加载可用；仍缺椭圆、圆环、线/点、屏蔽区和位置修正，模板训练仍依赖弹窗 |
| 3. 检测工具 | 部分实现 | Shape、Blob、灰度、边缘、HDevelop、数值判定、本地图像源、图像通道、图像滤波、阈值分割、Region 形态学、Region 特征筛选和 Region 集合运算已接入工具实例、顺序流程、独立/全流程运行；Image/Region 输出使用托管快照，首个双 Region 并/交/差/对称差汇聚可继续驱动筛选/Gray/Edge；仍缺数组、XLD/几何数据和完整视觉工具目录 |
| 4. TCP 通讯 | 部分实现 | Client/Server、手动收发、日志、结果自动发送；缺重连、心跳、超时、字段映射和流程触发 |
| 5. 结果与监控 | 部分实现 | 内存结果、回看、CSV/XLSX、OK/NG、良率、节拍、日志和报警；关闭后无持久历史 |
| 6. WPF 配方工作站 | 部分实现 | WPF 为活动入口，已形成 VM 主分区；流程范围、运行策略、实例参数、Image/Region 输入绑定、ROI 几何/锁定及图像/Region 工具参数可保存；中央卡片可见真实类型化端口、消费者和双输入汇聚，主要参数进入右侧停靠配置；Shape/HDevelop、二维连接画布、真实分支/Group、多流程和完整工程管理尚未迁移 |

## 已验证或有历史运行证据的基础能力

- WPF `MainWindow` 为启动入口。
- HALCON 20.11 运行时自检、图片加载、ROI、Shape Model、Blob、灰度和边缘流程有历史日志证据。
- TCP Client 的连接、接收和匹配结果发送有历史日志证据。
- CSV/XLSX 导出、截图、日志落盘和运行统计具备代码入口。

2026-07-19 S15 重新完成 Debug/Release x64 编译和双版本启动；中央流程真实 IN/OUT 端口、来源、值、消费者、等待/失效、删除和直接上下游高亮通过。双 Region 合成夹具得到并集 `2800`、交集 `400`、差集 `1200`、对称差 `2400 px²`，输出真实驱动特征筛选、Gray 和 Edge；缺一路、同源、类型/顺序、上游未运行、空交集、删除、重跑失效、快照释放和配方往返通过。S09-S14 与旧 v1 配方 `90/245/120 -> Schema 2` 全部回归；物理 1600×930（1942×1163 像素）窗口截图确认双输入汇聚卡、端口值/消费者、上下游状态、集合配置和黄色结果叠加。Shape Model 真实模板、相机和 `.jw/.handle` 旧工程仍缺样本/设备，未验证。

## 官方 VM 对标后的路线纠正

官方手册界面和 VM5.0 发布资料确认：开发主界面应以中央流程编辑为主，右侧分为图像和结果；流程长期需要网格布局、分支汇聚、自动串/并连排列和 Group 折叠；模块配置应形成统一的基本参数、运行参数、输入订阅、ROI/模板和结果显示工作台。

S04-S14 已建立主分区、常驻配置、ROI/逐区结果、类型化 Image/Region 数据链、可靠方案生命周期、分类模块箱、图像调试区和连续 HALCON 工具链；S15 已让真实端口依赖、消费者和双 Region 汇聚进入流程卡。下一阶段应把这些真实绑定升级为二维连接路径和依赖分层自动布局，同时增加通用 HALCON 图像增强；中央流程在真实调度模型完成前仍只宣称确定性线性执行，不把分层视觉布局冒充并行或 Group。

## P0：S06 ROI 配置工作台 + 逐 ROI 工业结果（已验证实现）

- A 线：把统一配置工作台的 `ROI/模板` 占位页升级为真实 ROI 绑定编辑器，显示图层名称、形状、显隐、启停和绑定状态，并能从配置工作台进入右上图像区绘制/管理；右上 ROI 侧栏同步显示当前工具上下文与结果状态。
- B 线：为 Blob、灰度和边缘增加兼容默认的“合并 ROI / 逐 ROI”运行模式；逐 ROI 模式按稳定 RoiId 独立执行并输出集合结果、逐区 OK/NG、数值、错误和耗时，进入结果区、日志和配方，不改变原合并模式及 HALCON 参数含义。
- 验证：统一配置草稿在应用前不污染实例，应用后绑定切换与 `PerRoi` 保存到实例；空绑定给出可操作错误，非法模式被拒绝，旧/缺失字段规范化为 `Union`。合成图两 ROI 的 Blob 为 `1200/600`、Gray 为 `121.829/211.917`（OK/NG）、Edge 为 `184.217/138.216`，集合端口均为 2；原 Union 全图路径仍为面积 `1200` 且无集合。Debug/Release 0 错误 0 警告，MainWindow 88 个、配置窗口 7 个事件处理器无缺失，Debug 保持运行 8 秒，ROI 工作台高 DPI 截图通过。
- BSB：按用户指令继续未实施/未验证。

## P0：S07 输入订阅工作台 + 真实 Region 数据链（已验证实现）

- A 线：统一配置的 `输入` 页已升级为逐端口订阅编辑器，显示类型、必需/可选、来源工具/端口、当前值和状态，支持草稿选择、应用/取消、恢复默认和跳转 I/O；流程节点和 I/O 页同步真实来源及等待/异常状态。
- B 线：实例输入绑定 Schema 已进入配方；Blob 输出安全托管的 `SelectedRegion`，后续 Gray/Edge 可订阅该 Region 作为真实运行域，同时保留本地 ROI/全图兼容路径。前向连接、类型不匹配、上游未运行/NG、失效快照和 Region 与逐 ROI 模式冲突均被拒绝；上游重跑/删除会使下游结果失效并释放旧快照。
- 验收：100×60 合成图中 Blob 输出 `Region ×2，Area=800`；Gray 全图基线均值 `26.667`，订阅后均值 `200`；Edge 订阅后长度 `96.108`。Gray/Edge 同时订阅、上游重跑失效、NG 阻断、类型/顺序错误、配置草稿应用/取消、配方绑定回读、旧配方缺字段、NumericJudge 输入编辑、对象释放、Debug/Release、启动、事件入口和高 DPI 输入页截图通过。
- BSB：按用户指令继续未实施/未验证。

## P0：S08 方案/配方工作区 + 自动保存恢复（已验证实现）

- A 线：顶部已同步方案名和 `新方案/未保存/已保存/可恢复` 状态；右下方案工作区提供 `当前/最近/恢复/自检` 四个内页，集中显示名称、正式路径、格式版本、最近保存时间，支持新建、打开、保存、另存、最近双击打开/移除和恢复/忽略。
- B 线：配方新增兼容 `SchemaVersion=v2`；编辑变化通过统一指纹判断脏状态并防抖写入独立恢复副本。正式保存采用同目录临时文件、回读验证、原子替换和单份 `.bak`；文件锁失败时正式文件字节不变。启动仅呈现较新的恢复副本，损坏副本隔离，不静默覆盖正式文件；`.jw/.handle` 未触碰。
- 验收：UI 生命周期测试覆盖脏状态、自动恢复副本、正式保存清脏、最近列表、启动发现和恢复；服务测试覆盖首次/覆盖保存、备份、锁文件失败保护、旧版无字段迁移、含 S07 输入绑定的恢复回读和损坏隔离。S07 HALCON 链保持 `Blob=800 / Gray=200 / Edge=96.108 / Region×2`；Debug/Release 0 警告 0 错误，MainWindow 88 个、配置窗口 10 个事件入口无缺失，Debug 启动 8 秒保持响应，1600×950 当前/最近方案页截图通过。
- BSB：按用户指令继续未实施/未验证。

## P0：S09 图像上下文工作台 + 本地图像源/Image 数据链（已验证实现）

- A 线：右上图像区已提供“全局输入/当前模块输入/当前模块输出”，显示真实来源工具/端口、文件/SN、尺寸、鼠标坐标、灰度/RGB 值与叠加状态；选中模块时自动定位可用图像，用户手动选择后保持选择，无输出时明确回退到模块输入/全局输入。
- B 线：模块箱和统一配置已新增“本地图像源”，可选择文件、设置 SN，输出安全托管的 `Image/Path/SN/Width/Height`；Blob/Gray/Edge 可订阅该 Image 真实运行，默认 `系统.Image` 路径保持兼容。前向、类型、上游状态、缺失/释放快照均校验，上游重跑/删除/配方重载释放旧 HImage 并使下游失效；未触碰相机 SDK。
- 验收：100×60 两张图经真实 Image 链得到 Blob `400→800`、Gray `17→34`、Edge `76.109→116.109`；Region 链均值 `255`、Number 链 `OK→OK`。类型/顺序/未运行/已释放、重跑失效、快照释放、参数/SN/绑定配方回读和图像上下文通过；Debug/Release 0 警告 0 错误，Debug 启动 8 秒响应，MainWindow 94 个、配置窗口 11 个事件入口无缺失，物理 1600×950 UI 截图通过。
- BSB：按用户指令继续未实施/未验证。

## P0：S10 停靠式模块配置 + HALCON 图像通道工具（已验证实现）

- A 线：右下已增加常驻“模块配置”工作台，本地图像源、图像通道、Blob、灰度、边缘和数值判定的基本/运行/输入/ROI/结果/帮助均在停靠页；流程双击、配置按钮和 I/O 参数入口直接定位实例，支持草稿、应用、撤销、试运行和切换保护。原兼容参数页已从正常路径隐藏，Shape/HDevelop 继续使用明确的兼容入口。
- B 线：新增 HALCON“图像通道”工具，可订阅 Image，配置保持原图、转灰度或抽取指定通道，输出托管 `Image`、输入/输出通道数、尺寸和结果码；下游 Blob/Gray/Edge 可继续订阅，参数和连接进入配方，重跑/异常/释放沿用 S09 生命周期。
- 验收：主要视觉参数无需弹窗即可完成“选择模块 -> 编辑草稿 -> 试运行 -> 应用/撤销 -> 查看 I/O/图像/结果 -> 保存回读”；`本地图像源.Image -> 图像通道.Image -> Blob/Gray/Edge` 已真实运行，RGB 转灰、三通道抽取、单通道兼容、非法通道、上游失效、对象释放和 S09 回归通过；Debug/Release 0 警告 0 错误，Debug 启动 8 秒响应，MainWindow 101 个、兼容配置窗口 11 个事件入口无缺失，物理 1600×950 UI 通过。
- BSB：按用户指令继续未实施/未验证。

## P0：S11 ROI 精细编辑 + HALCON 图像滤波（已验证实现）

- A 线：把右上 ROI 从“只新增/删除”推进到可选中精细编辑，首批覆盖矩形和圆的可见控制点、拖动移动、尺寸调整、复制与锁定；停靠配置的 ROI 页同步当前选中图层、锁定和几何摘要，配方保持稳定 RoiId。
- B 线：新增 HALCON“图像滤波”工具，订阅前序 Image，首批提供均值和中值滤波及受校验的模板尺寸，输出托管 Image、尺寸、模式和结果码；可继续连接图像通道与 Blob/Gray/Edge，配置、输入绑定、异常和快照生命周期进入现有闭环。
- 验收：矩形/圆命中、移动、尺寸/半径调整、最小尺寸与边界、复制独立 RoiId/保留工具绑定、锁定删除保护和锁定配方回读通过；原 Union/PerRoi 不回退。HALCON 3×3 均值冲击像素 `28`、中值半径 1 为 `0`、RGB 保持 3 通道，滤波下游 Blob `1267`、Gray `45.047`、Edge `139.152`；偶数模板和负半径被拒绝，上游失效与快照释放通过。Debug/Release 0 警告 0 错误，Release 启动 8 秒响应，128 个 XAML 事件入口无缺失，物理 1580×920（1942×1150 像素）双截图通过。
- 边界：不触碰相机 SDK、HALCON 既有工具参数含义、`.jw/.handle` 或 BSB。

## P0：S12 VM 模块箱效率化 + HALCON 阈值分割（已验证实现）

- A 线：把左侧模块箱从平铺列表升级为分类折叠、收藏、最近使用和拖拽添加；双击保持兼容，拖入中央确定性线性流程时显示插入位置，不伪造自由 Graph 或并行语义。
- B 线：新增 HALCON“阈值分割”工具，订阅前序 Image，配置灰度下限/上限并输出托管 Region、面积、区域数和结果码；Gray/Edge 可继续把该 Region 作为运行域，参数、连接、结果、日志、配方和生命周期闭环。
- 验收：搜索/分类折叠/收藏/最近/双击/拖拽插入具有真实 UI 路径，收藏与最近进入本机 `ui-state.json` 而不进入配方；指定第 2 步插入和序号刷新通过。RGB 两目标阈值得到 `Area=1792 / RegionCount=2`，Gray/Edge 订阅 Region 通过；空区域、非法范围、彩色转灰、顺序/类型、上游状态、释放、配方和旧配方回归通过；Debug/Release、启动和 1600×930 物理 UI 截图通过。
- 边界：本轮不引入真实分支/汇聚，不触碰相机 SDK、投产协议、`.jw/.handle` 或 BSB。

## P0：S13 图像调试区可视化 + HALCON Region 形态学（已验证实现）

- A 线：提升右上图像调试区的有效可视面积，增加工作区放大/恢复与 ROI/结果叠加的独立显示控制、当前结果图例和明确空状态；不以隐藏流程语义换取画布空间。
- B 线：新增 HALCON Region 形态学工具，订阅前序 Region，至少支持圆形开/闭、膨胀和腐蚀及可校验半径，输出托管 `Region/Area/RegionCount/Operation/ResultCode`，并可继续驱动 Gray/Edge 的 ROI。
- 验收：图像区放大/恢复、ROI 与结果叠加切换和图例有物理 UI 证据；`本地图像源.Image -> 图像滤波.Image -> 阈值分割.Region -> Region 形态学.Region -> Gray/Edge.ROI` 真实运行，四种模式、非法半径、空/NG 上游、顺序/类型、配方和生命周期通过。
- 边界：不触碰相机 SDK、投产协议、坐标约定、`.jw/.handle` 或 BSB。

## P0：S14 高级 ROI 编辑 + HALCON Region 特征筛选（已验证实现）

- A 线：旋转矩形和多边形已具备可见控制点与直接编辑，覆盖选择、移动、旋转/轴长、多边形顶点、复制、锁定、越界/最小尺寸/自相交保护、右侧摘要和配方回读；矩形/圆及 Union/PerRoi 未回退。
- B 线：HALCON“Region 特征筛选”已可订阅前序 Region，按面积、宽度、高度和圆度配置范围并输出托管 `Region/Area/RegionCount/Feature/Range/ResultCode`；Gray/Edge 可继续订阅。
- 验收：四类 ROI 编辑与配方、四类特征、非法范围、空/NG 上游、顺序/类型、下游失效和生命周期有自动化/物理 UI 证据，S09-S13 全部回归。
- 边界：不触碰相机 SDK、投产协议、坐标约定、`.jw/.handle` 或 BSB；不把单流程线性链冒充并行 Graph。

## P0：S15 流程依赖可视化 + HALCON Region 集合运算（已验证实现）

- A 线：中央流程卡已显示真实类型化输入/输出端口、来源、当前值和连接状态；选中模块高亮直接依赖/消费者，双 Region 输入以真实绑定展示汇聚关系，删除、重排和运行失效即时同步。
- B 线：HALCON“Region 集合运算”已配置两个必需 Region 输入，支持并集、交集、差集和对称差，输出托管 `Region/Area/RegionCount/Operation/ResultCode` 并驱动 Region 特征筛选/Gray/Edge。
- 验收：双来源在流程、输入页、I/O 与执行校验一致；四种面积 `2800/400/1200/2400`、缺一路/同源/类型/顺序/未运行、空结果、删除、配方和生命周期通过，S09-S14 与旧配方不回退，物理 UI 通过。
- 边界：仍为确定性线性调度，不宣称并行执行；未触碰相机 SDK、投产协议、坐标约定、`.jw/.handle` 或 BSB。S15 是上一 Git 批次第 `10/10` 次。

## P0：S16 真实连接画布首版 + HALCON 图像增强（提议，下一轮）

- A 线：把 S15 的真实端口和 `InputBindings` 映射为中央二维流程连接路径，按依赖层自动放置串联、分支和汇聚模块；连接命中、选中、类型颜色、缩放/平移、重排和删除均与真实绑定同步。序号继续表示确定性执行顺序，不宣称并行调度或 Group。
- B 线：新增 HALCON“图像增强”工具，首批提供线性灰度缩放和局部对比度增强，参数有范围/单位/默认值，输出托管 `Image/Width/Height/Channels/Mode/Parameters/ResultCode` 并可继续驱动阈值、Blob、Gray 和 Edge。
- 验收：至少一条分支和一处双 Region 汇聚在二维画布以真实连接路径可辨认，布局重算不改变执行/配方；灰度及 RGB 增强结果可核对，非法参数、上游状态、配方和生命周期闭环，S09-S15 不回退。
- 边界：不触碰相机 SDK、投产协议、坐标约定、`.jw/.handle` 或 BSB；二维布局仅表达真实数据依赖，不等于并行执行。

## P0：S05 统一模块配置工作台 + 实例参数 Schema（已验证实现）

- A 线：双击或配置入口打开统一 VM 类型模块配置工作台，提供基本参数、运行参数、输入、ROI/模板、结果显示和帮助页，并具备试运行、应用、确定和取消；右下 Inspector 保留为摘要和快捷入口。
- B 线：建立实例级参数 Schema 和验证/迁移契约，首批覆盖 Blob、灰度、边缘和数值判定；解除“同类型只能一个实例”的限制，旧配方顶层参数迁移到兼容实例，不改变既有 HALCON 参数含义。
- 首版边界：不在本轮迁移 Shape 模板训练窗体，不更改 HDevEngine 调用契约；未迁移工具仍使用兼容配置面板并明确标识。
- 验证：Blob 两实例参数 `20/110/33` 与 `140/250/77` 序列化回读并保持隔离；真实旧配方夹具迁移为 `90/245/120`；应用、取消、试运行恢复和非法范围拒绝通过；HALCON 回归为面积 `2000`、灰度均值 `50`、边缘长度 `176.109`；Debug/Release 0 错误 0 警告，88 个 XAML 事件入口无缺失，启动和 1920×1200 配置窗口截图通过。
- 边界：Shape/HDevelop 仍使用兼容配置页；输入、ROI/模板页当前只显示摘要和既有入口；缺少真实 `.jw/.handle` 与 Shape 模板样本，未验证。
- BSB：按用户指令未实施/未验证。

## P0：S04 官方 VM 主工作区 + 流程运行策略（已验证实现）

- A 线：活动 WPF 已重排为顶部菜单/快捷工具条、左侧模块箱、中央网格流程、右上 HALCON 图像/ROI、右下模块结果/配置和底部状态/历史；流程1 页签、真实顺序连线、缩放/适应、选择态和运行态可见。
- B 线：已实现运行当前、运行到此、从此运行、单次、连续、暂停/恢复、停止、连续间隔、流程超时和遇 NG 停止；运行范围、当前工具、总耗时、停止原因和错误进入右下结果、状态栏与日志。
- 配方兼容：新增可选 `FlowRunPolicy`；旧配方缺失时使用间隔 `500 ms`、超时 `0`（关闭）、遇 NG 不停止的兼容默认值，新策略保存/回读通过。
- 执行边界：HALCON 工具仍按确定性线性顺序同步执行；暂停、停止和超时在工具步骤之间生效，未宣称并行、汇聚或 Group。
- 验证：范围规划、禁用过滤、无选择异常、策略规范化和运行决策通过；旧/新配方回读通过；Debug/Release x64 0 错误 0 警告；Release 保持运行；物理 1920×1200 主分区截图完整。
- BSB：按用户指令未实施/未验证。

## P0：S03 VM 图像工具栏 + 工具级多 ROI（已验证实现）

- A 线：中央图像区新增选择、平移、矩形、圆形、多边形、确认、取消、适应、1:1 和图层显隐工具栏；图像侧栏显示 ROI 序号、名称、形状、显隐、启停和工具绑定摘要，右侧“图像/ROI”页提供命名、删除和绑定操作。
- B 线：Shape、Blob、灰度、边缘和 HDevelop 分别保存 ROI ID；运行时只解析该实例绑定且启用的图层，用 HALCON `Union2` 合并为 Region 后进入既有算子。Blob/灰度/边缘无绑定时保留全图行为，Shape/HDevelop 缺少有效绑定时明确拒绝。
- 配方兼容：新增 `RoiLayers` 和 `ToolFlow[].RoiIds`；继续写入首个有效 ROI 到旧 `SearchRoi`，S01/S02 或只有 `SearchRoi` 的配方自动迁移并绑定到原视觉工具，无 ROI 的旧配方保持无 ROI。
- 验证：两块白色区域合并 Blob 面积 `500`，停用一个图层后为 `100`；完整 `Blob → NumericJudge` 分别得到 OK/NG；JSON 与界面保存回读保持 2 图层/2 绑定；旧 `SearchRoi` 迁移为 1 图层/1 绑定；未绑定必需 ROI 被拒绝。
- 回归与视觉：Blob/灰度/边缘继续为 `3850 / 51.133 / 246.109`；79 个 XAML 事件入口无缺失；1920×1200 下两 ROI 同时叠加、中央工具栏和两处图层管理入口完整显示；Debug/Release 0 错误 0 警告，Release 启动通过。
- BSB：按用户指令继续暂停，本 Sprint 未实施或验证 BSB 专用功能。

## P0：S02 VM I/O 端口 + 数值判定（已验证实现）

- A 线：右侧 I/O 页已升级为 IN/OUT 端口卡片，显示端口名、类型、来源、当前值和连接状态；流程卡显示连接摘要，参数页可直接切换 I/O。
- B 线：新增“逻辑/计算 / 数值判定”工具，可订阅位于其前方的 Shape 分数/数量/坐标、Blob 面积、灰度均值、边缘长度或 HDevelop 数值，支持区间内、区间外、大于等于、小于等于、相等和不等判定。
- 工业闭环：配置、自动绑定、类型校验、顺序校验、独立运行、全流程运行、OK/NG、错误、耗时、结果 JSON 和配方持久化均已接通。
- 验证：合成图 Blob `Area=3850`；区间 `[3000,4000]` 判定 OK、`[4000,5000]` 判定 NG；Blob/灰度/边缘回归为 `3850 / 51.133 / 246.109`；错误顺序、错误区间和缺失值均拒绝；Debug/Release 0 错误 0 警告，Release 启动通过。
- BSB：按用户指令继续暂停，本 Sprint 未实施或验证 BSB 专用功能。

## 已完成基线：S01 VM 顺序流程工作区

主题：**VM 顺序流程工作区 + 通用视觉流程闭环**。

- A 线：已交付左侧可搜索工具箱、带序号的顺序流程实例区、中央 HALCON 图像区、右侧选中工具 Inspector 和底部结果区；支持添加、选择、启停、重命名、排序和响应式布局。
- B 线：已交付真实可执行的通用视觉流程模型，Shape/Blob/Gray/Edge/HDevelop 可按界面顺序独立或全流程运行，状态/结果/耗时/错误可见，顺序和启停状态写入配方。
- 兼容边界：未改变现有 HALCON 算法和参数语义；无 `ToolFlow` 的旧 JSON 配方按原五个布尔字段和原顺序恢复已启用工具；切换到无模板配方时主动清除旧模板，避免配方串用。
- BSB 状态：按用户 2026-07-13 指令暂缓，未在本 Sprint 实施。

## P0-P1：VM 完整产品主线

1. `P0` S06：统一 ROI 配置、图像区上下文和逐 ROI/集合结果已验证实现。
2. `P0` S07：输入订阅工作台、实例输入 Schema 和首条真实 Region 数据连接已验证实现。
3. `P0` S11/S14：矩形/圆/旋转矩形/多边形控制点、移动、几何调整、复制和锁定已验证实现；椭圆/圆环、屏蔽区和位置修正后续继续。
4. `P0` S08：方案/配方工作区、版本、迁移、另存、原子保存、自动保存和崩溃恢复已验证实现；多流程仍按真实执行模型另行推进。
5. `P0` S09：图像上下文工作台、本地图像源工具和首条跨工具 Image 数据链已验证实现。
6. `P0` S10：主要模块配置去弹窗化、右侧停靠工作台和 HALCON 图像通道工具已验证实现。
7. `P0` S12：模块箱分类/收藏/最近/拖拽确定性插入与 HALCON 阈值分割已验证实现。
8. `P0` S13：图像调试区有效面积、叠加管理与 HALCON Region 形态学已验证实现。
9. `P0` S14：旋转矩形/多边形 ROI 精细编辑与 HALCON Region 特征筛选已验证实现。
10. `P0` S15：真实类型化端口依赖可视化与 HALCON Region 双输入集合运算已验证实现。
11. `P0` S16：真实连接画布、依赖分层自动布局首版与 HALCON 图像增强。
12. `P0` 后续资源：相机/控制器/IO/光源资源页与活动 WPF 采集闭环；触碰相机 SDK 稳定边界前请求授权。
13. `P1` 后续通信：通信设备页、收发事件、报文监视、心跳、重连、超时、协议帧、字段映射、触发和回复。
14. `P1` 后续全局逻辑：全局变量、全局触发、全局脚本、数据队列和多流程执行。
15. `P1` 后续运行：运行界面设计器、生产运行模式、角色权限、历史持久化、图片回放、诊断、异常采集和版本追踪。
16. `P1-P2` 持续扩展 HALCON 定位、测量、识别、缺陷、标定、坐标、逻辑和通信工具目录。

## P2：BSB 核心能力（按用户指令暂停）

1. 双 Mark 管理与焊房复拍补偿。
2. 极柱点集、点位排序、数量和越界防呆。
3. 多模组缓存与 SN/载具号绑定。
4. 焊前到焊房、相机到机械轴、压脚、振镜和焊接坐标链。
5. PLC 握手、OK/NG 复位、地址配置和坐标批量发送。
6. 首件、补焊、焊接顺序与项目诊断。
7. CSV/MES 追溯。

## P3：平台化与运维

- Modbus TCP/RTU、Siemens S7、Omron CIP/EtherNet-IP、UDP、串口和自定义协议。
- 全局变量、表达式、数组、字符串、条件、缓存、计数、延时和脚本工具。
- 用户权限、生产模式、多语言、License、插件管理、数据库、MES 和系统诊断。
- 自定义模块生成、SDK 二次开发、插件版本和部署工具。
- 多相机、深度图和点云必须具备真实数据、显示和 HALCON 闭环，不接受空入口。

## 路线图维护规则

每轮结束按真实代码、编译、启动和功能验证结果更新；未从 UI 使用、不可独立测试或只有内部类的能力不得进入“已验证实现”。
