namespace HalconWinFormsDemo
{
    internal static class Program
    {
        // The WPF App.xaml generated entry point is now the application startup path.
        // This class remains only to avoid breaking older project references.
    }
}
